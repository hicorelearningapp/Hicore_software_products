<UserControl x:Class="EquipmentSimulator.View.TraceDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentSimulator.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== TRACE LIST ===== -->
        <GroupBox Header="Available Traces" Grid.Column="0">
            <DataGrid ItemsSource="{Binding Traces}"
                      SelectedItem="{Binding SelectedTrace}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TRID"
                                        Binding="{Binding TraceId}"
                                        Width="80"/>
                    <DataGridTextColumn Header="State"
                                        Binding="{Binding State}"
                                        Width="100"/>
                    <DataGridTextColumn Header="Period"
                                        Binding="{Binding PeriodMs}"
                                        Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- ===== DETAILS ===== -->
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Latest -->
            <GroupBox Header="Latest Values">
                <DataGrid ItemsSource="{Binding LatestSamples}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="VID" Binding="{Binding Vid}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120"/>
                        <DataGridTextColumn Header="Time" Binding="{Binding TimeUtc}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- History -->
            <GroupBox Header="Recent History" Grid.Row="1">
                <DataGrid ItemsSource="{Binding SampleHistory}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="VID" Binding="{Binding Vid}" Width="80"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120"/>
                        <DataGridTextColumn Header="Time" Binding="{Binding TimeUtc}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>

</UserControl>
