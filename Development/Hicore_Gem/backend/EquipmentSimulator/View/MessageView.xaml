<UserControl x:Class="EquipmentSimulator.View.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentSimulator.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding MessageLog}"
                 Background="White"
                 Foreground="OrangeRed"
                 FontFamily="Consolas"
                 FontSize="14"
                 BorderThickness="1"
                 BorderBrush="Gray" />

        <Button Grid.Row="1"  Height="20" Width="100">Clear</Button>

    </Grid>
</UserControl>
