<UserControl x:Class="HostSimulator.View.SingleMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HostSimulator.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="1"
        BorderBrush="Gray"
        Padding="10"
        Margin="10"
        CornerRadius="6">

        <StackPanel Orientation="Vertical" >
            <StackPanel Orientation="Horizontal"> 
            <!-- Label -->
            <TextBlock Text="Send SECS Message"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>
               
                <!-- ComboBox for SxFy commands -->
                <ComboBox Width="200" Margin="100,0,0,0"
              ItemsSource="{Binding Commands}"
              SelectedItem="{Binding SelectedCommand}" />
                
            </StackPanel>
            <!-- MULTI-LINE TEXT EDIT BOX -->
            <TextBox x:Name="MessageTextBox"
                 Text="{Binding OutgoingMessage, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Height="200"
                 Margin="0 0 0 10"/>

            <StackPanel Orientation="Horizontal">
            <!-- SEND BUTTON -->
            <Button Content="Send Message"
                Width="150"
                Height="35"
                HorizontalAlignment="Left"
                Margin="0 10 0 0"  Command="{Binding SendMessageCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>

</UserControl>
